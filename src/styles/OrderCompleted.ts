import styled from "styled-components";

export const OrderContent = styled.div`

.imageThank{
    position: relative;
    align-items: right;
    width: 350px;
    height: 200px;

    p{
        position: absolute;
        bottom: 45px;
        right: -15px;
        font-weight: bold;
        color: var(--Blue)
    }
}
`;