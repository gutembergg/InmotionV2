import styled from "styled-components";

export const SliderProductImage = styled.div`
  .imgBlock {
    position: relative;
    width: 100%;
    max-width: 400px;
    height: 200px;
    margin-bottom: 10px;
    border-bottom: 2px solid var(--Blue);
  }

  .swiperThumb {
    width: 90%;
    max-width: 400px;
    margin: 0 auto;
  }
`;
